---
title: "Windows 的一些设置"
published: 2025-12-02T15:31:00
description: '使用 S3 睡眠模式 reg add HKLM\System\CurrentControlSet\Control\Power /v PlatformAoAcOverride /t REG_DWORD '
tags: []
category: Blog
---

## 使用 S3 睡眠模式

```
reg add HKLM\System\CurrentControlSet\Control\Power /v PlatformAoAcOverride /t REG_DWORD /d 0
```

## 开机默认启用 NumLock

注册表 `HKEY_USERS\.DEFAULT\Control Panel\Keyboard` 键 `InitialKeyboardIndicators` 值改为 `2`。

## 开启和关闭临时 IPv6 地址

关闭
```
netsh interface ipv6 set privacy state=disabled
```

开启
```
netsh interface ipv6 set privacy state=enabled
```

## 禁止鼠标唤醒

到设备管理器里，除了鼠标要禁用唤醒，还要把一个叫键盘的也禁用唤醒。